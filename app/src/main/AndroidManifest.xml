<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lasiya.breach">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
 If your app targets Android 9 or lower, you can declare
         ACCESS_COARSE_LOCATION instead.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:name="com.lasiya.breach.SocialDistanceAlarm"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        tools:replace="android:theme, android:icon"
        android:theme="@style/AppTheme">
        <!--Check orientation and process under here if problem-->
        <activity android:name="com.lasiya.breach.UnityHolderActivity"
            android:screenOrientation="portrait"
            android:process=":UnityProcess"
            />
        <activity android:name="com.lasiya.breach.ChooseActivity" />
        <activity android:name="com.lasiya.breach.LandingActivity" />
        <activity android:name="com.lasiya.breach.LoginActivity" />
        <activity android:name="com.lasiya.breach.RegisterActivity" />
        <activity android:name="com.lasiya.breach.Biometric" />

        <receiver
            android:name="com.lasiya.opentracesdk_fat.alarm.BLEClient"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name="com.lasiya.opentracesdk_fat.alarm.BLEServer"
            android:enabled="true"
            android:exported="false" />
        <receiver android:name="com.lasiya.breach.alarm.AutoStart">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.lasiya.breach.MainActivity"

            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>